# Legacy Python Experiments

This folder preserves the older Python PPO, TorchRL, checkpoint, model-gameplay,
scripted-baseline, local-combat analysis, notebook, and dataset-generation work.

The active CPC path now lives in:

- `configs/env/`
- `scripts/manual_env_debug.py`
- `experiment/core/`
- `experiment/baselines/aim_oracle/`
- `experiment/baselines/move_score/`
- `experiment/baselines/tactical_baseline/`
- `experiment/tests/`

Legacy files are reference material and may need path/import repair before they
can be run again.
