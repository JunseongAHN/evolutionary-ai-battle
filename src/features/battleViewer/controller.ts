// Thin controller placeholder for future battle-viewer interactions.
export default class BattleViewerController {
    state: unknown;

    constructor(state: unknown = null) {
        this.state = state;
    }
}
